<UserControl
	x:Class="Depot.Views.CurrentUserView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
		<Grid Margin="32" MaxWidth="980" HorizontalAlignment="Left">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="64" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<Border Width="56" Height="56" CornerRadius="28" Background="{DynamicResource PrimaryBrush}" VerticalAlignment="Top">
					<TextBlock Text="{Binding Initials}" Foreground="{DynamicResource PrimaryForegroundBrush}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
				</Border>

				<StackPanel Grid.Column="1" Margin="12,3,24,0">
					<TextBlock Text="{Binding User.DisplayName}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="22" FontWeight="SemiBold" />
					<TextBlock Text="{Binding User.Email}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,4,0,0" />
					<TextBlock Text="{Binding RolesSummary}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,3,0,0" />
				</StackPanel>

				<Button
					Grid.Column="2"
					Content="Sign out"
					Command="{Binding DataContext.LogoutCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
					VerticalAlignment="Top"
					Padding="12,6" />
			</Grid>

			<Grid Grid.Row="1" Margin="0,28,0,0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="20" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="18">
					<StackPanel>
						<TextBlock Text="Account" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,14" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="150" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>
							<TextBlock Text="User ID" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Column="1" Text="{Binding User.Id}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="1" Text="Display name" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding User.DisplayName}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="2" Text="Email" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding User.Email}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="3" Text="Active" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding User.IsActive}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="4" Text="Created" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding User.CreatedUtc, StringFormat={}{0:yyyy-MM-dd HH:mm} UTC}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="5" Text="Version" Foreground="{DynamicResource SecondaryTextBrush}" />
							<TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding User.Version}" Foreground="{DynamicResource PrimaryTextBrush}" />
						</Grid>
					</StackPanel>
				</Border>

				<Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="18">
					<StackPanel>
						<TextBlock Text="Authorization" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,14" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="190" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>
							<TextBlock Text="Administrator" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Column="1" Text="{Binding User.IsAdministrator}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="1" Text="Purchase order approval" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding User.CanApprovePurchaseOrders}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="2" Text="Primary role" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding User.Role}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10" />
							<TextBlock Grid.Row="3" Text="Assigned roles" Foreground="{DynamicResource SecondaryTextBrush}" />
							<TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding RolesSummary}" Foreground="{DynamicResource PrimaryTextBrush}" TextWrapping="Wrap" />
						</Grid>
					</StackPanel>
				</Border>
			</Grid>

			<Border Grid.Row="2" Margin="0,20,0,0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="18">
				<StackPanel>
					<TextBlock Text="Roles" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" />
					<ItemsControl ItemsSource="{Binding Roles}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Grid Margin="0,0,0,10">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="180" />
										<ColumnDefinition Width="130" />
										<ColumnDefinition Width="*" />
										<ColumnDefinition Width="90" />
									</Grid.ColumnDefinitions>
									<TextBlock Text="{Binding Name}" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold" />
									<TextBlock Grid.Column="1" Text="{Binding Code}" Foreground="{DynamicResource SecondaryTextBrush}" />
									<TextBlock Grid.Column="2" Text="{Binding Description}" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap" />
									<TextBlock Grid.Column="3" Text="{Binding IsSystem, StringFormat=System: {0}}" Foreground="{DynamicResource SecondaryTextBrush}" />
								</Grid>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</StackPanel>
			</Border>

			<Border Grid.Row="3" Margin="0,20,0,32" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="18">
				<StackPanel>
					<TextBlock Text="Effective permissions" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14" FontWeight="SemiBold" />
					<TextBlock Text="{Binding EffectivePermissions.Count, StringFormat={}{0} permissions}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,3,0,12" />
					<ItemsControl ItemsSource="{Binding EffectivePermissions}">
						<ItemsControl.ItemsPanel>
							<ItemsPanelTemplate>
								<WrapPanel />
							</ItemsPanelTemplate>
						</ItemsControl.ItemsPanel>
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Border Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,6,6">
									<TextBlock Text="{Binding}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" />
								</Border>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</StackPanel>
			</Border>
		</Grid>
	</ScrollViewer>
</UserControl>
