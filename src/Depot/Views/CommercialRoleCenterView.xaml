<UserControl x:Class="Depot.Views.CommercialRoleCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Depot.Controls">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PageHeader Title="{Binding Title}" Subtitle="{Binding Subtitle}" />

        <controls:OperationPanel Grid.Row="1"
                                 ErrorText="{Binding OperationError}"
                                 HasError="{Binding HasOperationError}"
                                 IsBusy="{Binding IsBusy}"
                                 StatusText="{Binding StatusText}" />

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <ItemsControl ItemsSource="{Binding Kpis}" Visibility="{Binding HasKpis, Converter={StaticResource BooleanToVisibility}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource WorkspaceSectionStyle}" Width="190" Margin="0,0,10,10">
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" Style="{StaticResource CaptionStyle}" />
                                <TextBlock Text="{Binding Value}" FontSize="22" FontWeight="SemiBold" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding SupportingText}" Style="{StaticResource CaptionStyle}" TextWrapping="Wrap" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Orientation="Horizontal" Visibility="{Binding HasQuickActions, Converter={StaticResource BooleanToVisibility}}">
                <ItemsControl ItemsSource="{Binding QuickActions}">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Label}" Click="OnQuickActionClick" Style="{StaticResource PrimaryButtonStyle}" Padding="14,8" MinWidth="128" Margin="0,0,8,0" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <TextBox Text="{Binding DecisionComment, UpdateSourceTrigger=PropertyChanged}"
                     Visibility="{Binding IsApprovalInbox, Converter={StaticResource BooleanToVisibility}}"
                     Margin="0,10,0,0"
                     MinWidth="320"
                     ToolTip="Optional approval comment or rejection reason" />
        </StackPanel>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Sections}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource WorkspaceSectionStyle}" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" Style="{DynamicResource WorkspaceSectionTitleStyle}" Margin="0,0,0,8" />
                                <Grid>
                                    <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True"
                                              CanUserAddRows="False" SelectionMode="Single" SelectionUnit="FullRow"
                                              MouseDoubleClick="OnItemsDoubleClick" KeyDown="OnItemsKeyDown" MinHeight="120" MaxHeight="280">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Type / Work" Binding="{Binding Title}" Width="170" />
                                            <DataGridTextColumn Header="Reference" Binding="{Binding DisplayNumber}" Width="140" />
                                            <DataGridTextColumn Header="Requester / Creator" Binding="{Binding Requester}" Width="140" />
                                            <DataGridTextColumn Header="Counterparty / Context" Binding="{Binding Context}" Width="*" />
                                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="100" />
                                            <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="75" />
                                            <DataGridTextColumn Header="Submitted / Activity" Binding="{Binding SubmittedDisplay}" Width="145" />
                                            <DataGridTextColumn Header="Due" Binding="{Binding DueDisplay}" Width="95" />
                                            <DataGridTextColumn Header="Age" Binding="{Binding AgeDisplay}" Width="75" />
                                            <DataGridTextColumn Header="Match / Allocation" Binding="{Binding StateDetail}" Width="145" />
                                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="135" />
                                            <DataGridTextColumn Header="Next Action" Binding="{Binding NextAction}" Width="145" />
                                            <DataGridTemplateColumn Header="" Width="220">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Button Content="Open" Click="OnOpenClick" Style="{StaticResource SecondaryButtonStyle}" Padding="8,3" Margin="0,0,5,0" />
                                                            <Button Content="Approve" Click="OnApproveClick" Style="{StaticResource PrimaryButtonStyle}" Padding="8,3" Margin="0,0,5,0"
                                                                    Visibility="{Binding CanApprove, Converter={StaticResource BooleanToVisibility}}" />
                                                            <Button Content="Reject" Click="OnRejectClick" Style="{StaticResource SecondaryButtonStyle}" Padding="8,3"
                                                                    Visibility="{Binding CanReject, Converter={StaticResource BooleanToVisibility}}" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <TextBlock Text="{Binding EmptyText}" Style="{StaticResource CaptionStyle}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibility}}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
