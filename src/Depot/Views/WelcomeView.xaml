<UserControl x:Class="Depot.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Width="760" HorizontalAlignment="Center" Margin="0,72,0,48">
                <Image Source="/Resources/Images/Depot_32.png" Width="48" Height="48" HorizontalAlignment="Left" />
                <TextBlock Text="{Binding Greeting}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="30" FontWeight="SemiBold" Margin="0,22,0,0" />
                <TextBlock Text="{Binding Subtitle}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="16" Margin="0,6,0,0" />
                <TextBlock Text="Choose a module from the activity bar or use one of these shortcuts to get started." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,20,0,0" />

                <TextBlock Text="Getting started" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="15" FontWeight="SemiBold" Margin="0,34,0,12" />
                <ItemsControl ItemsSource="{Binding Tips}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16,13" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="130" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Shortcut}" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Consolas" FontSize="11" />
                                    </Border>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="12" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Description}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>