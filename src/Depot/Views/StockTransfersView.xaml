<UserControl x:Class="Depot.Views.StockTransfersView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Depot.Controls"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><controls:OperationPanel ErrorText="{Binding OperationError}" HasError="{Binding HasOperationError}" HelpTopicId="warehouse.transfers" IsBusy="{Binding IsBusy}" StatusText="{Binding StatusText}"/><controls:MasterDetailGrid Grid.Row="1"><controls:MasterDetailGrid.Master><controls:Card><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><controls:FilterBar><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="6"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><controls:SearchBox Placeholder="Search number, warehouse or notes" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/><ComboBox Grid.Row="2" DisplayMemberPath="Name" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" Style="{StaticResource AppComboBoxStyle}"/></Grid></controls:FilterBar><Grid Grid.Row="1"><ListBox ItemContainerStyle="{StaticResource MasterDataListBoxItemStyle}" ItemsSource="{Binding Transfers}" SelectedItem="{Binding SelectedTransfer}" Style="{StaticResource MasterDataListBoxStyle}"><ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock FontWeight="SemiBold" Text="{Binding TransferNumber}"/><TextBlock Style="{StaticResource CaptionStyle}"><Run Text="{Binding SourceWarehouseName}"/><Run Text=" → "/><Run Text="{Binding DestinationWarehouseName}"/></TextBlock></StackPanel><StackPanel Grid.Column="1" Margin="8,0,0,0"><controls:DocumentStatusBadge HorizontalAlignment="Right" Status="{Binding StatusDisplayName}"/><TextBlock Margin="0,3,0,0" HorizontalAlignment="Right" Style="{StaticResource CaptionStyle}" Text="{Binding TransferDate, StringFormat={}{0:d}}"/></StackPanel></Grid></DataTemplate></ListBox.ItemTemplate></ListBox><controls:WorkflowListState ItemCount="{Binding Transfers.Count}" SearchText="{Binding SearchText}" IsBusy="{Binding IsBusy}" HasError="{Binding HasOperationError}" ErrorText="{Binding OperationError}"/></Grid><Grid Grid.Row="2" Margin="0,10,0,0"><controls:PaginationControl PreviousCommand="{Binding PreviousPageCommand}" NextCommand="{Binding NextPageCommand}" PageText="{Binding PageDisplay}"/><Button HorizontalAlignment="Right" Command="{Binding NewTransferCommand}" Content="New transfer" Style="{StaticResource PrimaryButtonStyle}"/></Grid></Grid></controls:Card></controls:MasterDetailGrid.Master><controls:MasterDetailGrid.Detail><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel><controls:Card><controls:Card.Header><controls:WorkflowHeader Title="{Binding EditorTitle}" Subtitle="{Binding Draft.TransferNumber}" Status="{Binding Draft.Status}"/></controls:Card.Header><StackPanel><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="165"/></Grid.ColumnDefinitions><StackPanel><TextBlock Style="{StaticResource FormLabelStyle}" Text="Source warehouse"/><ComboBox DisplayMemberPath="Name" IsEnabled="{Binding IsDraft}" ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedSourceWarehouse}" Style="{StaticResource AppComboBoxStyle}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Style="{StaticResource FormLabelStyle}" Text="Destination warehouse"/><ComboBox DisplayMemberPath="Name" IsEnabled="{Binding IsDraft}" ItemsSource="{Binding DestinationWarehouses}" SelectedItem="{Binding SelectedDestinationWarehouse}" Style="{StaticResource AppComboBoxStyle}"/></StackPanel><StackPanel Grid.Column="4"><TextBlock Style="{StaticResource FormLabelStyle}" Text="Transfer date"/><DatePicker IsEnabled="{Binding IsDraft}" SelectedDate="{Binding Draft.TransferDate}" Style="{StaticResource AppDatePickerStyle}"/></StackPanel></Grid><TextBlock Margin="0,10,0,0" Style="{StaticResource FormLabelStyle}" Text="Notes"/><TextBox IsReadOnly="{Binding IsReadOnly}" MinHeight="52" Style="{StaticResource MultilineTextBoxStyle}" Text="{Binding Draft.Notes, UpdateSourceTrigger=PropertyChanged}"/><DataGrid MinHeight="180" Margin="0,12,0,0" ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLine}" Style="{StaticResource AppDataGridStyle}"><DataGrid.Columns><DataGridTextColumn Binding="{Binding LineNumber}" Header="#" Width="40"/><DataGridTextColumn Binding="{Binding ItemDisplay}" Header="Item" Width="*"/><DataGridTextColumn Binding="{Binding SourceDisplay}" Header="Source inventory" Width="180"/><DataGridTextColumn Binding="{Binding DestinationDisplay}" Header="Destination inventory" Width="180"/><DataGridTextColumn Binding="{Binding Quantity, StringFormat={}{0:N0}}" Header="Qty" Width="65" ElementStyle="{StaticResource AppDataGridNumericTextStyle}"/><DataGridTextColumn Binding="{Binding AvailableStock, StringFormat={}{0:N0}}" Header="Avail." Width="65" ElementStyle="{StaticResource AppDataGridNumericTextStyle}"/><DataGridTextColumn Binding="{Binding RemainingStock, StringFormat={}{0:N0}}" Header="Remain." Width="70" ElementStyle="{StaticResource AppDataGridNumericTextStyle}"/></DataGrid.Columns></DataGrid><Border Margin="0,10,0,0" Style="{StaticResource WorkspaceToolbarBorderStyle}" IsEnabled="{Binding IsDraft}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="90"/><ColumnDefinition Width="10"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ComboBox ItemsSource="{Binding SourceInventoryOptions}" SelectedItem="{Binding SelectedSourceInventory}" DisplayMemberPath="DisplayName" Style="{StaticResource AppComboBoxStyle}"/><ComboBox Grid.Column="2" ItemsSource="{Binding DestinationInventoryOptions}" SelectedItem="{Binding SelectedDestinationInventory}" DisplayMemberPath="DisplayName" Style="{StaticResource AppComboBoxStyle}"/><controls:TextInput Grid.Column="4" Text="{Binding LineQuantity, UpdateSourceTrigger=PropertyChanged}"/><StackPanel Grid.Column="6" Orientation="Horizontal"><Button Command="{Binding RemoveLineCommand}" Content="Remove" Style="{StaticResource SecondaryButtonStyle}"/><Button Margin="{StaticResource Button.Margin}" Command="{Binding AddLineCommand}" Content="{Binding SaveLineText}" Style="{StaticResource SecondaryButtonStyle}"/></StackPanel></Grid></Border><controls:WorkflowActionBar><controls:WorkflowActionBar.SecondaryActions><StackPanel Orientation="Horizontal"><Button Command="{Binding CancelTransferCommand}" Content="Cancel draft" Style="{StaticResource DangerButtonStyle}"/><Button Margin="{StaticResource Button.Margin}" Command="{Binding SaveTransferCommand}" Content="Save draft" Style="{StaticResource SecondaryButtonStyle}"/></StackPanel></controls:WorkflowActionBar.SecondaryActions><controls:WorkflowActionBar.PrimaryAction><Button Command="{Binding PostTransferCommand}" Content="Post transfer" Style="{StaticResource PrimaryButtonStyle}"/></controls:WorkflowActionBar.PrimaryAction></controls:WorkflowActionBar></StackPanel></controls:Card><controls:Card Margin="0,12,0,0" Visibility="{Binding HasMovements, Converter={StaticResource BooleanToVisibilityConverter}}"><controls:Card.Header><TextBlock Style="{StaticResource SectionTitleStyle}" Text="Generated transfer movements"/></controls:Card.Header><DataGrid MinHeight="160" ItemsSource="{Binding Movements}" Style="{StaticResource AppDataGridStyle}"><DataGrid.Columns><DataGridTextColumn Binding="{Binding TimestampLocal, StringFormat={}{0:g}}" Header="Time" Width="130"/><DataGridTextColumn Binding="{Binding MovementType}" Header="Direction" Width="90"/><DataGridTextColumn Binding="{Binding PartNumber}" Header="Item" Width="105"/><DataGridTextColumn Binding="{Binding Description}" Header="Description" Width="*"/><DataGridTextColumn Binding="{Binding WarehouseName}" Header="Warehouse" Width="110"/><DataGridTextColumn Binding="{Binding LocationName}" Header="Location" Width="105"/><DataGridTextColumn Binding="{Binding Quantity, StringFormat={}{0:N0}}" Header="Qty" Width="65"/></DataGrid.Columns></DataGrid></controls:Card><controls:Card Margin="0,12,0,0" Visibility="{Binding CanReverse, Converter={StaticResource BooleanToVisibilityConverter}}"><controls:Card.Header><TextBlock Style="{StaticResource SectionTitleStyle}" Text="Reverse posted transfer"/></controls:Card.Header><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ComboBox ItemsSource="{Binding ReversalReasonCodes}" SelectedItem="{Binding SelectedReversalReasonCode}" DisplayMemberPath="Name" Style="{StaticResource AppComboBoxStyle}"/><controls:TextInput Grid.Column="2" Text="{Binding ReversalReason, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="4" Command="{Binding ReverseTransferCommand}" Content="Reverse transfer" Style="{StaticResource DangerButtonStyle}"/></Grid></controls:Card></StackPanel></ScrollViewer></controls:MasterDetailGrid.Detail></controls:MasterDetailGrid></Grid></UserControl>