<UserControl
	x:Class="Depot.Views.ItemsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="16" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<TextBlock
				Grid.Column="0"
				Text="Items"
				FontSize="22"
				FontWeight="SemiBold" />

			<Button
				Grid.Column="1"
				Width="120"
				Height="32"
				Content="New Item"
				Command="{Binding NewItemCommand}" />

		</Grid>

		<Grid Grid.Row="2">

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="2*" />
				<ColumnDefinition Width="24" />
				<ColumnDefinition Width="1*" />
			</Grid.ColumnDefinitions>

			<DataGrid
				Grid.Column="0"
				ItemsSource="{Binding Items}"
				SelectedItem="{Binding SelectedItem}"
				AutoGenerateColumns="False"
				IsReadOnly="True"
				CanUserAddRows="False"
				CanUserDeleteRows="False">

				<DataGrid.Columns>

					<DataGridTextColumn
						Header="Part Number"
						Binding="{Binding PartNumber}"
						Width="180" />

					<DataGridTextColumn
						Header="Description"
						Binding="{Binding Description}"
						Width="*" />

					<DataGridTextColumn
						Header="Manufacturer"
						Binding="{Binding Manufacturer}"
						Width="180" />

					<DataGridTextColumn
						Header="Category"
						Binding="{Binding Category}"
						Width="180" />

				</DataGrid.Columns>

			</DataGrid>

			<Border
				Grid.Column="2"
				Padding="16"
				BorderBrush="#DDDDDD"
				BorderThickness="1">

				<Grid>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="16" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="16" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="8" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="8" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="8" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="16" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>

					<TextBlock
						Grid.Row="0"
						Text="{Binding Editor.EditorTitle}"
						FontSize="18"
						FontWeight="SemiBold" />

					<Border
						Grid.Row="2"
						Padding="8"
						Background="#FFF3F3"
						BorderBrush="#CC5555"
						BorderThickness="1"
						Visibility="{Binding HasErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}">

						<TextBlock
							Text="{Binding ErrorMessage}"
							TextWrapping="Wrap" />

					</Border>

					<StackPanel Grid.Row="4">
						<TextBlock Text="Part Number" />
						<TextBox
							Margin="0,4,0,0"
							IsEnabled="{Binding Editor.CanEditPartNumber}"
							Text="{Binding Editor.PartNumber, UpdateSourceTrigger=PropertyChanged}" />
					</StackPanel>

					<StackPanel Grid.Row="6">
						<TextBlock Text="Description" />
						<TextBox
							Margin="0,4,0,0"
							Text="{Binding Editor.Description, UpdateSourceTrigger=PropertyChanged}" />
					</StackPanel>

					<StackPanel Grid.Row="8">
						<TextBlock Text="Manufacturer" />
						<TextBox
							Margin="0,4,0,0"
							Text="{Binding Editor.Manufacturer, UpdateSourceTrigger=PropertyChanged}" />
					</StackPanel>

					<StackPanel Grid.Row="10">
						<TextBlock Text="Category" />
						<TextBox
							Margin="0,4,0,0"
							Text="{Binding Editor.Category, UpdateSourceTrigger=PropertyChanged}" />
					</StackPanel>

					<StackPanel
						Grid.Row="12"
						Orientation="Horizontal"
						HorizontalAlignment="Right">

						<Button
							Width="100"
							Height="32"
							Margin="0,0,8,0"
							Content="Deactivate"
							Command="{Binding DeactivateItemCommand}" />

						<Button
							Width="100"
							Height="32"
							Content="{Binding Editor.SaveButtonText}"
							Command="{Binding SaveItemCommand}" />

					</StackPanel>

				</Grid>

			</Border>

		</Grid>

	</Grid>

</UserControl>