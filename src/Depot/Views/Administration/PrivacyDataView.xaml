<UserControl
	x:Class="Depot.Views.Administration.PrivacyDataView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid Margin="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<StackPanel>
			<TextBlock FontSize="24" FontWeight="SemiBold" Text="Privacy data" />
			<TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Text="Locate personal data stored in Depot and create a machine-readable discovery export. Results are an administrative aid and do not override legal retention requirements." />
		</StackPanel>

		<StackPanel Grid.Row="1" Margin="0,18,0,12" Orientation="Horizontal">
			<TextBox Width="360" MinHeight="32" VerticalContentAlignment="Center" AutomationProperties.Name="Personal data search" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
			<Button Margin="10,0,0,0" MinWidth="90" Content="Search" Command="{Binding SearchCommand}" />
			<Button Margin="10,0,0,0" MinWidth="110" Content="Export JSON" Command="{Binding ExportCommand}" />
		</StackPanel>

		<TextBlock Grid.Row="2" Margin="0,0,0,10" Text="{Binding GeneratedDisplay}" />

		<DataGrid Grid.Row="3" ItemsSource="{Binding Records}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column" AutomationProperties.Name="Personal data matches">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150" />
				<DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="150" />
				<DataGridTextColumn Header="ID" Binding="{Binding EntityId}" Width="80" />
				<DataGridTextColumn Header="Subject" Binding="{Binding Subject}" Width="2*" />
				<DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*" />
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
