<UserControl
	x:Class="Depot.Views.Administration.AdministrationView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:Depot.Controls">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<controls:PageHeader
			Margin="{DynamicResource Page.Header.BottomMargin}"
			Title="Administration"
			Subtitle="Configure and maintain your Depot installation." />

		<ListBox
			Grid.Row="1"
			Margin="{DynamicResource SecondaryNavigation.Margin}"
			ItemsSource="{Binding NavigationItems}"
			SelectedItem="{Binding SelectedNavigationItem}"
			Style="{DynamicResource SecondaryNavigationListBoxStyle}"
			ItemContainerStyle="{DynamicResource SecondaryNavigationItemStyle}">
			<ListBox.ItemsPanel>
				<ItemsPanelTemplate>
					<StackPanel Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</ListBox.ItemsPanel>
			<ListBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<ContentControl Grid.Row="2" Content="{Binding CurrentViewModel}" />
	</Grid>
</UserControl>
