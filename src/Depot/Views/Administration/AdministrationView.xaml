<UserControl x:Class="Depot.Views.Administration.AdministrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Depot.Controls"
             xmlns:administrationViewModels="clr-namespace:Depot.ViewModels.Administration"
             xmlns:administrationViews="clr-namespace:Depot.Views.Administration">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type administrationViewModels:UserSessionsViewModel}">
            <administrationViews:UserSessionsView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{DynamicResource BackgroundBrush}">
        <controls:ShellModuleContentControl HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch"
                                            Content="{Binding CurrentViewModel}" />
    </Grid>
</UserControl>
