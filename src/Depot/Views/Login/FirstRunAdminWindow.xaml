<Window
	x:Class="Depot.Views.Login.FirstRunAdminWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationFramework"
	xmlns:controls="clr-namespace:Depot.Controls"
	Title="Secure Depot setup"
	Icon="/Resources/Images/Depot.ico"
	Style="{StaticResource AppDialogWindowStyle}"
	WindowStartupLocation="CenterScreen"
	ShowInTaskbar="true"
	ResizeMode="NoResize">
	<Border Padding="24" Background="{DynamicResource BackgroundBrush}">
		<controls:Card Width="420">
			<StackPanel>
				<Image Width="64" Height="56" HorizontalAlignment="Center" Margin="0,0,0,16" Source="/Resources/Images/Depot.png" automation:AutomationProperties.AccessibilityView="Raw" />
				<TextBlock HorizontalAlignment="Center" Text="Create the first administrator" TextAlignment="Center" Style="{StaticResource PageTitleStyle}" />
				<TextBlock Margin="0,4,0,20" HorizontalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Text="Secure this database with an individual administrator account before continuing." TextAlignment="Center" TextWrapping="Wrap" Style="{StaticResource BodyStyle}" />

				<TextBlock x:Name="DisplayNameLabel" Text="Display name" Style="{StaticResource FormLabelStyle}" />
				<controls:TextInput x:Name="DisplayNameBox" automation:AutomationProperties.LabeledBy="{Binding ElementName=DisplayNameLabel}" automation:AutomationProperties.IsRequiredForForm="True" />

				<TextBlock x:Name="EmailLabel" Margin="0,12,0,4" Text="Email" Style="{StaticResource FormLabelStyle}" />
				<controls:TextInput x:Name="EmailBox" automation:AutomationProperties.LabeledBy="{Binding ElementName=EmailLabel}" automation:AutomationProperties.IsRequiredForForm="True" />

				<TextBlock x:Name="PasswordLabel" Margin="0,12,0,4" Text="Password" Style="{StaticResource FormLabelStyle}" />
				<controls:PasswordInput x:Name="PasswordBox" automation:AutomationProperties.LabeledBy="{Binding ElementName=PasswordLabel}" automation:AutomationProperties.IsRequiredForForm="True" automation:AutomationProperties.HelpText="Password must satisfy all requirements listed below." Style="{StaticResource RoundedPasswordInputStyle}" PasswordValueChanged="PasswordInput_Changed" />
				<StackPanel Margin="0,7,0,0" automation:AutomationProperties.Name="Password requirements">
					<TextBlock x:Name="LengthRequirementText" Text="○ 12–128 characters" Style="{StaticResource CaptionStyle}" />
					<TextBlock x:Name="UppercaseRequirementText" Margin="0,2,0,0" Text="○ At least one uppercase letter" Style="{StaticResource CaptionStyle}" />
					<TextBlock x:Name="LowercaseRequirementText" Margin="0,2,0,0" Text="○ At least one lowercase letter" Style="{StaticResource CaptionStyle}" />
					<TextBlock x:Name="DigitRequirementText" Margin="0,2,0,0" Text="○ At least one number" Style="{StaticResource CaptionStyle}" />
					<TextBlock x:Name="SymbolRequirementText" Margin="0,2,0,0" Text="○ At least one symbol" Style="{StaticResource CaptionStyle}" />
					<TextBlock x:Name="AccountNameRequirementText" Margin="0,2,0,0" Text="○ Must not contain the account name" Style="{StaticResource CaptionStyle}" />
				</StackPanel>

				<TextBlock x:Name="ConfirmPasswordLabel" Margin="0,12,0,4" Text="Confirm password" Style="{StaticResource FormLabelStyle}" />
				<controls:PasswordInput x:Name="ConfirmPasswordBox" automation:AutomationProperties.LabeledBy="{Binding ElementName=ConfirmPasswordLabel}" automation:AutomationProperties.IsRequiredForForm="True" Style="{StaticResource RoundedPasswordInputStyle}" PasswordValueChanged="PasswordInput_Changed" />
				<TextBlock x:Name="PasswordMatchText" Margin="0,6,0,0" Foreground="{DynamicResource SecondaryTextBrush}" Text="○ Re-enter the password to confirm it." TextWrapping="Wrap" Style="{StaticResource CaptionStyle}" automation:AutomationProperties.LiveSetting="Polite" />

				<Border Margin="0,12,0,0" Padding="10,8" Background="{DynamicResource ErrorBrush}" BorderBrush="{DynamicResource ErrorForegroundBrush}" BorderThickness="1" CornerRadius="{DynamicResource CornerRadius.Medium}" Visibility="Collapsed" x:Name="ErrorPanel" automation:AutomationProperties.LiveSetting="Assertive">
					<TextBlock x:Name="ErrorText" Foreground="{DynamicResource ErrorForegroundBrush}" TextWrapping="Wrap" Style="{StaticResource CaptionStyle}" />
				</Border>

				<Button x:Name="CreateButton" Margin="0,20,0,0" HorizontalAlignment="Stretch" Content="Create administrator" IsDefault="True" Style="{StaticResource PrimaryButtonStyle}" Click="CreateAdministrator_Click" />
			</StackPanel>
		</controls:Card>
	</Border>
</Window>
