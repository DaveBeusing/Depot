<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:Depot.Controls">

	<Style TargetType="{x:Type controls:StatusBadge}">
		<Setter Property="Padding" Value="10,4" />
		<Setter Property="HorizontalAlignment" Value="Left" />
		<Setter Property="VerticalAlignment" Value="Center" />
		<Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
		<Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
		<Setter Property="FontSize" Value="11" />
		<Setter Property="FontWeight" Value="SemiBold" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:StatusBadge}">
					<Border
						Background="{TemplateBinding Background}"
						CornerRadius="10"
						Padding="{TemplateBinding Padding}"
						SnapsToDevicePixels="True">
						<ContentPresenter
							HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
							VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
							RecognizesAccessKey="True" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="Variant" Value="Success">
							<Setter Property="Background" Value="{DynamicResource SuccessBrush}" />
							<Setter Property="Foreground" Value="{DynamicResource SuccessForegroundBrush}" />
						</Trigger>
						<Trigger Property="Variant" Value="Warning">
							<Setter Property="Background" Value="{DynamicResource WarningBrush}" />
							<Setter Property="Foreground" Value="{DynamicResource WarningForegroundBrush}" />
						</Trigger>
						<Trigger Property="Variant" Value="Error">
							<Setter Property="Background" Value="{DynamicResource ErrorBrush}" />
							<Setter Property="Foreground" Value="{DynamicResource ErrorForegroundBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.5" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type controls:ConnectionStatusIndicator}">
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
		<Setter Property="DetailForeground" Value="{DynamicResource SecondaryTextBrush}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:ConnectionStatusIndicator}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="8" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<Ellipse
							x:Name="StatusDot"
							Width="8"
							Height="8"
							VerticalAlignment="Center"
							Fill="{DynamicResource ErrorForegroundBrush}" />

						<StackPanel Grid.Column="2">
							<TextBlock
								Foreground="{TemplateBinding Foreground}"
								FontSize="11"
								FontWeight="SemiBold"
								Text="{TemplateBinding Status}"
								TextTrimming="CharacterEllipsis" />
							<TextBlock
								x:Name="DetailText"
								Margin="0,2,0,0"
								Foreground="{TemplateBinding DetailForeground}"
								FontSize="11"
								Text="{TemplateBinding Detail}"
								TextTrimming="CharacterEllipsis" />
						</StackPanel>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="State" Value="Connected">
							<Setter TargetName="StatusDot" Property="Fill" Value="{DynamicResource SuccessForegroundBrush}" />
						</Trigger>
						<Trigger Property="State" Value="Pending">
							<Setter TargetName="StatusDot" Property="Fill" Value="{DynamicResource WarningForegroundBrush}" />
						</Trigger>
						<Trigger Property="Compact" Value="True">
							<Setter TargetName="DetailText" Property="Visibility" Value="Collapsed" />
						</Trigger>
						<Trigger Property="Detail" Value="">
							<Setter TargetName="DetailText" Property="Visibility" Value="Collapsed" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type controls:OperationStatus}">
		<Setter Property="Visibility" Value="Collapsed" />
		<Setter Property="Margin" Value="0,0,0,12" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:OperationStatus}">
					<Border
						x:Name="Container"
						Padding="12,8"
						Background="{DynamicResource SurfaceAltBrush}"
						CornerRadius="6">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="120" />
								<ColumnDefinition Width="12" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<ProgressBar
								x:Name="Progress"
								Height="3"
								VerticalAlignment="Center"
								IsIndeterminate="True" />
							<TextBlock
								x:Name="Message"
								Grid.Column="2"
								VerticalAlignment="Center"
								Foreground="{DynamicResource SecondaryTextBrush}"
								Text="{TemplateBinding StatusText}" />
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsBusy" Value="True">
							<Setter Property="Visibility" Value="Visible" />
						</Trigger>
						<Trigger Property="HasError" Value="True">
							<Setter Property="Visibility" Value="Visible" />
							<Setter TargetName="Container" Property="Background" Value="{DynamicResource ErrorBrush}" />
							<Setter TargetName="Message" Property="Foreground" Value="{DynamicResource ErrorForegroundBrush}" />
							<Setter TargetName="Message" Property="Text" Value="{Binding ErrorText, RelativeSource={RelativeSource TemplatedParent}}" />
							<Setter TargetName="Progress" Property="Visibility" Value="Collapsed" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
