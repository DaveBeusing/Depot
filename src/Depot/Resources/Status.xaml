<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Base status badge -->

	<Style
		x:Key="StatusBadgeStyle"
		TargetType="Border">

		<Setter
			Property="CornerRadius"
			Value="10" />

		<Setter
			Property="Padding"
			Value="10,4" />

		<Setter
			Property="HorizontalAlignment"
			Value="Left" />

		<Setter
			Property="VerticalAlignment"
			Value="Center" />

	</Style>

	<!-- Active -->

	<Style
		x:Key="ActiveStatusBadgeStyle"
		TargetType="Border"
		BasedOn="{StaticResource StatusBadgeStyle}">

		<Setter
			Property="Background"
			Value="{DynamicResource SuccessBrush}" />

	</Style>

	<!-- Inactive -->

	<Style
		x:Key="InactiveStatusBadgeStyle"
		TargetType="Border"
		BasedOn="{StaticResource StatusBadgeStyle}">

		<Setter
			Property="Background"
			Value="{DynamicResource BorderBrush}" />

	</Style>

	<!-- Status text -->

	<Style
		x:Key="StatusBadgeTextStyle"
		TargetType="TextBlock">

		<Setter
			Property="FontSize"
			Value="11" />

		<Setter
			Property="FontWeight"
			Value="SemiBold" />

		<Setter
			Property="Foreground"
			Value="{DynamicResource PrimaryTextBrush}" />

	</Style>

</ResourceDictionary>