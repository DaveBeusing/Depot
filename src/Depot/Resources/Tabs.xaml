<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:Depot.Controls">
	<Style x:Key="AppTabItemStyle" TargetType="TabItem">
		<Setter Property="MinHeight" Value="32" />
		<Setter Property="Margin" Value="0,0,18,0" />
		<Setter Property="Padding" Value="2,7" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
		<Setter Property="BorderBrush" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="FontSize" Value="12" />
		<Setter Property="FontWeight" Value="SemiBold" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="Cursor" Value="Hand" />
		<Setter Property="controls:MotionBehavior.TransitionKind" Value="State" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TabItem">
					<Grid>
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="HoverSurface" Storyboard.TargetProperty="Opacity" To="0" Duration="{controls:MotionDuration Kind=Fast}" />
									</Storyboard>
								</VisualState>
								<VisualState x:Name="MouseOver">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="HoverSurface" Storyboard.TargetProperty="Opacity" To="1" Duration="{controls:MotionDuration Kind=Fast}" />
									</Storyboard>
								</VisualState>
								<VisualState x:Name="Disabled">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="Surface" Storyboard.TargetProperty="Opacity" To="0.45" Duration="{controls:MotionDuration Kind=Fast}" />
										<DoubleAnimation Storyboard.TargetName="HoverSurface" Storyboard.TargetProperty="Opacity" To="0" Duration="{controls:MotionDuration Kind=Fast}" />
									</Storyboard>
								</VisualState>
							</VisualStateGroup>
							<VisualStateGroup x:Name="SelectionStates">
								<VisualState x:Name="Unselected">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="SelectionAccent" Storyboard.TargetProperty="Opacity" To="0" Duration="{controls:MotionDuration Kind=Fast}" />
										<DoubleAnimation Storyboard.TargetName="SelectionAccentScale" Storyboard.TargetProperty="ScaleX" To="0.45" Duration="{controls:MotionDuration Kind=Standard}" />
									</Storyboard>
								</VisualState>
								<VisualState x:Name="Selected">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="SelectionAccent" Storyboard.TargetProperty="Opacity" To="1" Duration="{controls:MotionDuration Kind=Fast}" />
										<DoubleAnimation Storyboard.TargetName="SelectionAccentScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="{controls:MotionDuration Kind=Standard}">
											<DoubleAnimation.EasingFunction>
												<CubicEase EasingMode="EaseOut" />
											</DoubleAnimation.EasingFunction>
										</DoubleAnimation>
									</Storyboard>
								</VisualState>
							</VisualStateGroup>
							<VisualStateGroup x:Name="FocusStates">
								<VisualState x:Name="Unfocused">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="{controls:MotionDuration Kind=Fast}" />
									</Storyboard>
								</VisualState>
								<VisualState x:Name="Focused">
									<Storyboard>
										<DoubleAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="Opacity" To="0.9" Duration="{controls:MotionDuration Kind=Fast}" />
									</Storyboard>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<Border x:Name="HoverSurface" Background="{DynamicResource HoverBrush}" CornerRadius="{DynamicResource Radius.S}" Opacity="0" IsHitTestVisible="False" />
						<Border x:Name="Surface" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
							<ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
						</Border>
						<Border
							x:Name="SelectionAccent"
							Height="2"
							VerticalAlignment="Bottom"
							Background="{DynamicResource PrimaryBrush}"
							Opacity="0"
							RenderTransformOrigin="0.5,0.5"
							IsHitTestVisible="False">
							<Border.RenderTransform>
								<ScaleTransform x:Name="SelectionAccentScale" ScaleX="0.45" ScaleY="1" />
							</Border.RenderTransform>
						</Border>
						<Border
							x:Name="FocusBorder"
							BorderBrush="{DynamicResource PrimaryBrush}"
							BorderThickness="1"
							CornerRadius="{DynamicResource Radius.S}"
							Opacity="0"
							IsHitTestVisible="False" />
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
						</Trigger>
						<Trigger Property="IsSelected" Value="True">
							<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Cursor" Value="Arrow" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="AppTabControlStyle" TargetType="TabControl">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Stretch" />
		<Setter Property="ItemContainerStyle" Value="{StaticResource AppTabItemStyle}" />
		<Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Contained" />
		<Setter Property="ItemsPanel">
			<Setter.Value>
				<ItemsPanelTemplate>
					<WrapPanel Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TabControl">
					<Grid KeyboardNavigation.TabNavigation="Local">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
							<ItemsPresenter KeyboardNavigation.TabIndex="1" />
						</Border>
						<ContentPresenter
							x:Name="PART_SelectedContentHost"
							Grid.Row="1"
							Margin="0,12,0,0"
							HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
							VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
							Content="{TemplateBinding SelectedContent}"
							ContentStringFormat="{TemplateBinding SelectedContentStringFormat}"
							ContentTemplate="{TemplateBinding SelectedContentTemplate}"
							ContentTemplateSelector="{TemplateBinding SelectedContentTemplateSelector}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="TabControl" BasedOn="{StaticResource AppTabControlStyle}" />
</ResourceDictionary>
