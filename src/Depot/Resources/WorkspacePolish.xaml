<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:Depot.Controls">

	<!-- Workspace layout -->
	<Thickness x:Key="Workspace.Padding">20</Thickness>
	<Thickness x:Key="Workspace.Section.Margin">0,0,0,16</Thickness>
	<Thickness x:Key="Workspace.Header.Margin">0,0,0,14</Thickness>
	<Thickness x:Key="Workspace.Toolbar.Padding">12,9</Thickness>
	<CornerRadius x:Key="Workspace.Toolbar.CornerRadius">8</CornerRadius>

	<!-- Compact surface used for filters, toolbars, and secondary information. -->
	<Style x:Key="WorkspaceToolbarBorderStyle" TargetType="Border">
		<Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="CornerRadius" Value="{StaticResource Workspace.Toolbar.CornerRadius}" />
		<Setter Property="Padding" Value="{StaticResource Workspace.Toolbar.Padding}" />
	</Style>

	<Style x:Key="WorkspaceTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource SectionTitleStyle}">
		<Setter Property="FontSize" Value="18" />
		<Setter Property="FontWeight" Value="SemiBold" />
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
	</Style>

	<Style x:Key="WorkspaceSubtitleStyle" TargetType="TextBlock" BasedOn="{StaticResource CaptionStyle}">
		<Setter Property="Margin" Value="0,4,0,0" />
		<Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
		<Setter Property="TextWrapping" Value="Wrap" />
	</Style>

	<!-- Cards inside workspaces are deliberately flatter than dashboard cards. -->
	<Style x:Key="WorkspaceCardStyle" TargetType="{x:Type controls:Card}" BasedOn="{StaticResource CardBaseStyle}">
		<Setter Property="Padding" Value="14" />
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
	</Style>

	<Style x:Key="WorkspaceSectionHeaderStyle" TargetType="TextBlock">
		<Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
		<Setter Property="FontSize" Value="11" />
		<Setter Property="FontWeight" Value="SemiBold" />
		<Setter Property="TextOptions.TextFormattingMode" Value="Display" />
	</Style>

</ResourceDictionary>
