<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:Depot.Controls">

	<SolidColorBrush x:Key="MetricCard.IconBackgroundBrush" Color="#EFF6FF" />
	<SolidColorBrush x:Key="MetricCard.IconBorderBrush" Color="#DBEAFE" />
	<DropShadowEffect
		x:Key="MetricCard.Shadow"
		BlurRadius="12"
		Direction="270"
		Opacity="0.08"
		ShadowDepth="2" />

	<Style TargetType="{x:Type controls:Card}">
		<Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Stretch" />
		<Setter Property="Padding" Value="{DynamicResource Card.Padding}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:Card}">
					<Border
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						CornerRadius="{DynamicResource Card.CornerRadius}"
						SnapsToDevicePixels="True">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>

							<Border
								x:Name="HeaderContainer"
								Grid.Row="0"
								Padding="{TemplateBinding Padding}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="0,0,0,1">
								<ContentPresenter
									Content="{TemplateBinding Header}"
									ContentTemplate="{TemplateBinding HeaderTemplate}"
									ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
									ContentStringFormat="{TemplateBinding HeaderStringFormat}"
									RecognizesAccessKey="True" />
							</Border>

							<ContentPresenter
								Grid.Row="1"
								Margin="{TemplateBinding Padding}"
								HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
								Content="{TemplateBinding Content}"
								ContentTemplate="{TemplateBinding ContentTemplate}"
								ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
								ContentStringFormat="{TemplateBinding ContentStringFormat}" />

							<Border
								x:Name="FooterContainer"
								Grid.Row="2"
								Padding="{TemplateBinding Padding}"
								BorderBrush="{TemplateBinding BorderBrush}"
								BorderThickness="0,1,0,0">
								<ContentPresenter
									Content="{TemplateBinding Footer}"
									ContentTemplate="{TemplateBinding FooterTemplate}" />
							</Border>
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="Header" Value="{x:Null}">
							<Setter TargetName="HeaderContainer" Property="Visibility" Value="Collapsed" />
						</Trigger>
						<Trigger Property="Footer" Value="{x:Null}">
							<Setter TargetName="FooterContainer" Property="Visibility" Value="Collapsed" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.6" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type controls:MetricCard}">
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
		<Setter Property="MinHeight" Value="132" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Stretch" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:MetricCard}">
					<controls:Card Effect="{StaticResource MetricCard.Shadow}">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>

							<StackPanel VerticalAlignment="Center">
								<ContentPresenter
									Content="{TemplateBinding Title}"
									TextElement.FontSize="14"
									TextElement.FontWeight="SemiBold"
									TextElement.Foreground="{DynamicResource SecondaryTextBrush}" />

								<ContentPresenter
									Margin="0,8,0,0"
									Content="{TemplateBinding Value}"
									ContentTemplate="{TemplateBinding ValueTemplate}"
									TextElement.FontSize="32"
									TextElement.FontWeight="SemiBold"
									TextElement.Foreground="{TemplateBinding Foreground}" />

								<ContentPresenter
									x:Name="SupportingTextPresenter"
									Margin="0,8,0,0"
									Content="{TemplateBinding SupportingText}"
									TextElement.FontSize="11"
									TextElement.Foreground="{DynamicResource SecondaryTextBrush}" />
							</StackPanel>

							<Border
								x:Name="IconPresenter"
								Grid.Column="1"
								Width="40"
								Height="40"
								Margin="16,0,0,0"
								VerticalAlignment="Top"
								Background="{StaticResource MetricCard.IconBackgroundBrush}"
								BorderBrush="{StaticResource MetricCard.IconBorderBrush}"
								BorderThickness="1"
								CornerRadius="8">
								<Path
									Width="18"
									Height="18"
									Data="{TemplateBinding Icon}"
									Stroke="{DynamicResource PrimaryBrush}"
									StrokeEndLineCap="Round"
									StrokeLineJoin="Round"
									StrokeThickness="1.6"
									Stretch="Uniform" />
							</Border>
						</Grid>
					</controls:Card>
					<ControlTemplate.Triggers>
						<Trigger Property="SupportingText" Value="{x:Null}">
							<Setter TargetName="SupportingTextPresenter" Property="Visibility" Value="Collapsed" />
						</Trigger>
						<Trigger Property="Icon" Value="{x:Null}">
							<Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.6" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
