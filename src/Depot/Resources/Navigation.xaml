<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Navigation List -->

	<Style
		x:Key="NavigationListBoxStyle"
		TargetType="ListBox">

		<Setter
			Property="Background"
			Value="Transparent" />

		<Setter
			Property="BorderThickness"
			Value="0" />

		<Setter
			Property="Foreground"
			Value="{StaticResource NavigationForegroundBrush}" />

	</Style>

	<!-- Navigation Item -->

	<Style
		x:Key="NavigationListBoxItemStyle"
		TargetType="ListBoxItem">

		<Setter
			Property="Margin"
			Value="{Binding Margin}" />

		<Setter
			Property="Padding"
			Value="12" />

		<Setter
			Property="HorizontalContentAlignment"
			Value="Stretch" />

		<Setter
			Property="Template">

			<Setter.Value>

				<ControlTemplate
					TargetType="ListBoxItem">

					<Border
						x:Name="Border"
						Background="Transparent"
						CornerRadius="6"
						Padding="{TemplateBinding Padding}">

						<ContentPresenter
							HorizontalAlignment="Stretch"
							VerticalAlignment="Center" />

					</Border>

					<ControlTemplate.Triggers>

						<Trigger
							Property="IsMouseOver"
							Value="True">

							<Setter
								TargetName="Border"
								Property="Background"
								Value="{StaticResource NavigationHoverBrush}" />

						</Trigger>

						<Trigger
							Property="IsSelected"
							Value="True">

							<Setter
								TargetName="Border"
								Property="Background"
								Value="{StaticResource NavigationSelectedBrush}" />

						</Trigger>

					</ControlTemplate.Triggers>

				</ControlTemplate>

			</Setter.Value>

		</Setter>

	</Style>

	<!-- Navigation Item Template -->

	<DataTemplate
		x:Key="NavigationItemTemplate">

		<StackPanel
			Orientation="Horizontal">

			<TextBlock
				Width="24"
				Text="{Binding Icon}"
				Style="{StaticResource NavigationTextStyle}" />

			<TextBlock
				Text="{Binding Name}"
				Style="{StaticResource NavigationTextStyle}" />

		</StackPanel>

	</DataTemplate>

</ResourceDictionary>