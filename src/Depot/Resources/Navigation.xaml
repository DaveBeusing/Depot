<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:Depot.Controls">

	<!--
		Depot UI Kit - Navigation

		Provides the complete styling for the main navigation ListBox and its
		items. All visual feedback is implemented with standard WPF visual
		states and storyboards. No external UI library is required.
	-->

	<!-- Animation timings -->
	<Duration x:Key="NavigationAnimationDuration.Fast">0:0:0.10</Duration>
	<Duration x:Key="NavigationAnimationDuration.Normal">0:0:0.16</Duration>

	<!-- Navigation List -->
	<Style x:Key="NavigationListBoxStyle" TargetType="ListBox">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderBrush" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="Foreground" Value="{DynamicResource NavigationForegroundBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.CanContentScroll" Value="True" />
		<Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
		<Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
		<Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Contained" />
		<Setter Property="KeyboardNavigation.TabNavigation" Value="Once" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListBox">
					<Border
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						SnapsToDevicePixels="True">
						<ScrollViewer
							Focusable="False"
							Padding="0"
							HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
							VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
							CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}">
							<ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Navigation Item -->
	<Style x:Key="NavigationListBoxItemStyle" TargetType="ListBoxItem">
		<Setter Property="MinHeight" Value="44" />
		<Setter Property="Margin" Value="0,2,0,2" />
		<Setter Property="Padding" Value="12,10" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderBrush" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="Foreground" Value="{DynamicResource NavigationForegroundBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="SnapsToDevicePixels" Value="True" />
		<Setter Property="UseLayoutRounding" Value="True" />
		<Setter Property="Cursor" Value="Hand" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListBoxItem">
					<Grid Background="Transparent" SnapsToDevicePixels="True">
						<VisualStateManager.VisualStateGroups>
							<!-- Pointer and availability states -->
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="HoverBackground"
											Storyboard.TargetProperty="Opacity"
											To="0"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
										<DoubleAnimation
											Storyboard.TargetName="Root"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
									</Storyboard>
								</VisualState>

								<VisualState x:Name="MouseOver">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="HoverBackground"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
										<DoubleAnimation
											Storyboard.TargetName="Root"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
									</Storyboard>
								</VisualState>

								<VisualState x:Name="Disabled">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="HoverBackground"
											Storyboard.TargetProperty="Opacity"
											To="0"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
										<DoubleAnimation
											Storyboard.TargetName="Root"
											Storyboard.TargetProperty="Opacity"
											To="0.42"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
									</Storyboard>
								</VisualState>
							</VisualStateGroup>

							<!-- Selection states -->
							<VisualStateGroup x:Name="SelectionStates">
								<VisualState x:Name="Unselected">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="SelectedBackground"
											Storyboard.TargetProperty="Opacity"
											To="0"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
										<DoubleAnimation
											Storyboard.TargetName="Accent"
											Storyboard.TargetProperty="Opacity"
											To="0"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
										<DoubleAnimation
											Storyboard.TargetName="AccentScale"
											Storyboard.TargetProperty="ScaleY"
											To="0.35"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
									</Storyboard>
								</VisualState>

								<VisualState x:Name="Selected">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="SelectedBackground"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
										<DoubleAnimation
											Storyboard.TargetName="Accent"
											Storyboard.TargetProperty="Opacity"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
										<DoubleAnimation
											Storyboard.TargetName="AccentScale"
											Storyboard.TargetProperty="ScaleY"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Normal}">
											<DoubleAnimation.EasingFunction>
												<CubicEase EasingMode="EaseOut" />
											</DoubleAnimation.EasingFunction>
										</DoubleAnimation>
									</Storyboard>
								</VisualState>

								<VisualState x:Name="SelectedUnfocused">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="SelectedBackground"
											Storyboard.TargetProperty="Opacity"
											To="0.78"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
										<DoubleAnimation
											Storyboard.TargetName="Accent"
											Storyboard.TargetProperty="Opacity"
											To="0.88"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
										<DoubleAnimation
											Storyboard.TargetName="AccentScale"
											Storyboard.TargetProperty="ScaleY"
											To="1"
											Duration="{StaticResource NavigationAnimationDuration.Normal}" />
									</Storyboard>
								</VisualState>
							</VisualStateGroup>

							<!-- Keyboard focus states -->
							<VisualStateGroup x:Name="FocusStates">
								<VisualState x:Name="Unfocused">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="FocusBorder"
											Storyboard.TargetProperty="Opacity"
											To="0"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
									</Storyboard>
								</VisualState>

								<VisualState x:Name="Focused">
									<Storyboard>
										<DoubleAnimation
											Storyboard.TargetName="FocusBorder"
											Storyboard.TargetProperty="Opacity"
											To="0.9"
											Duration="{StaticResource NavigationAnimationDuration.Fast}" />
									</Storyboard>
								</VisualState>
							</VisualStateGroup>

						</VisualStateManager.VisualStateGroups>

						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="4" />
							<ColumnDefinition Width="8" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<!-- Animated selection accent -->
						<Border
							x:Name="Accent"
							Grid.Column="0"
							Margin="0,4,0,4"
							Background="{DynamicResource NavigationAccentBrush}"
							CornerRadius="0,2,2,0"
							Opacity="0"
							RenderTransformOrigin="0.5,0.5">
							<Border.RenderTransform>
								<ScaleTransform x:Name="AccentScale" ScaleX="1" ScaleY="0.35" />
							</Border.RenderTransform>
						</Border>

						<!-- Item surface -->
						<Grid x:Name="Root" Grid.Column="2">

							<!-- Hover and selection are separate layers so their animations
							     never compete for the same property. -->
							<Border
								x:Name="HoverBackground"
								Background="{DynamicResource NavigationHoverBrush}"
								CornerRadius="6,0,0,6"
								Opacity="0" />

							<Border
								x:Name="SelectedBackground"
								Background="{DynamicResource NavigationSelectedBackgroundBrush}"
								CornerRadius="6,0,0,6"
								Opacity="0" />

							<Border
								x:Name="FocusBorder"
								Margin="1"
								BorderBrush="{DynamicResource NavigationAccentBrush}"
								BorderThickness="1"
								CornerRadius="5,0,0,5 "
								Opacity="0" />

							<ContentPresenter
								Margin="{TemplateBinding Padding}"
								HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
								Content="{TemplateBinding Content}"
								ContentTemplate="{TemplateBinding ContentTemplate}"
								ContentStringFormat="{TemplateBinding ContentStringFormat}"
								RecognizesAccessKey="True"
								SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

						</Grid>

					</Grid>

					<ControlTemplate.Triggers>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Cursor" Value="Arrow" />
						</Trigger>
					</ControlTemplate.Triggers>

				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Style.Triggers>
			<DataTrigger Binding="{Binding IsSeparated}" Value="True">
				<Setter Property="Margin" Value="0,22,0,2" />
			</DataTrigger>
		</Style.Triggers>
	</Style>

	<Style x:Key="MasterDataListBoxStyle" TargetType="ListBox">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
		<Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
	</Style>

	<Style x:Key="MasterDataListBoxItemStyle" TargetType="ListBoxItem">
		<Setter Property="Margin" Value="4" />
		<Setter Property="Padding" Value="{DynamicResource Control.Padding}" />
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource HoverBrush}" /></Trigger>
			<Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource NavigationSelectedBackgroundBrush}" /></Trigger>
		</Style.Triggers>
	</Style>

	<!-- Navigation Item Content -->
	<DataTemplate x:Key="NavigationItemTemplate">
		<Grid VerticalAlignment="Center">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="18" />
				<ColumnDefinition Width="12" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<Path
				Grid.Column="0"
				Width="18"
				Height="18"
				HorizontalAlignment="Left"
				VerticalAlignment="Center"
				Data="{Binding IconData}"
				Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
				StrokeEndLineCap="Round"
				StrokeLineJoin="Round"
				StrokeThickness="1.5"
				Stretch="Uniform" />

			<TextBlock
				Grid.Column="2"
				VerticalAlignment="Center"
				Text="{Binding Name}"
				TextTrimming="CharacterEllipsis"
				Style="{DynamicResource NavigationTextStyle}" />
		</Grid>
	</DataTemplate>

	<Style TargetType="{x:Type controls:SidebarBrand}">
		<Setter Property="Margin" Value="16,18,16,24" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:SidebarBrand}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="48" />
							<ColumnDefinition Width="12" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<Image
							Width="48"
							Height="48"
							Source="{TemplateBinding LogoSource}"
							Stretch="Uniform" />

						<StackPanel Grid.Column="2" VerticalAlignment="Center">
							<TextBlock
								Foreground="{DynamicResource NavigationForegroundBrush}"
								FontSize="20"
								FontWeight="SemiBold"
								Text="{TemplateBinding Title}"
								TextOptions.TextFormattingMode="Display"
								TextOptions.TextRenderingMode="ClearType" />
							<TextBlock
								Margin="0,2,0,0"
								Foreground="{DynamicResource NavigationMutedTextBrush}"
								FontSize="10"
								Text="{TemplateBinding Subtitle}"
								TextTrimming="CharacterEllipsis" />
						</StackPanel>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="SidebarLogoutButtonStyle" TargetType="Button">
		<Setter Property="Width" Value="30" />
		<Setter Property="Height" Value="30" />
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="Cursor" Value="Hand" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="ToolTip" Value="Log out" />
		<Setter Property="AutomationProperties.Name" Value="Log out" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">
					<Border
						x:Name="ButtonSurface"
						Background="{TemplateBinding Background}"
						CornerRadius="6">
						<Path
							Width="15"
							Height="15"
							Data="M 8,2 L 3,2 L 3,14 L 8,14 M 10,5 L 13,8 L 10,11 M 6,8 L 13,8"
							Stroke="{DynamicResource NavigationMutedTextBrush}"
							StrokeEndLineCap="Round"
							StrokeLineJoin="Round"
							StrokeThickness="1.5" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource NavigationHoverBrush}" />
						</Trigger>
						<Trigger Property="IsPressed" Value="True">
							<Setter TargetName="ButtonSurface" Property="Opacity" Value="0.7" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter TargetName="ButtonSurface" Property="Opacity" Value="0.4" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type controls:SidebarUserPanel}">
		<Setter Property="Margin" Value="12" />
		<Setter Property="Padding" Value="12" />
		<Setter Property="Background" Value="{DynamicResource NavigationUserPanelBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource NavigationUserPanelBorderBrush}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type controls:SidebarUserPanel}">
					<Border
						Padding="{TemplateBinding Padding}"
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						CornerRadius="10">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="36" />
								<ColumnDefinition Width="10" />
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="8" />
								<ColumnDefinition Width="30" />
							</Grid.ColumnDefinitions>

							<Border
								Width="36"
								Height="36"
								Background="{DynamicResource NavigationSelectedBackgroundBrush}"
								CornerRadius="18">
								<Path
									Width="16"
									Height="16"
									Data="M 8,8 A 3,3 0 1 0 8,2 A 3,3 0 1 0 8,8 M 2.5,15 C 3,11.5 5,10 8,10 C 11,10 13,11.5 13.5,15"
									Stroke="{DynamicResource NavigationAccentBrush}"
									StrokeEndLineCap="Round"
									StrokeThickness="1.5" />
							</Border>

							<StackPanel Grid.Column="2" VerticalAlignment="Center">
								<TextBlock
									Foreground="{DynamicResource NavigationForegroundBrush}"
									FontSize="13"
									FontWeight="SemiBold"
									Text="{TemplateBinding DisplayName}"
									TextTrimming="CharacterEllipsis" />
								<TextBlock
									Margin="0,2,0,0"
									Foreground="{DynamicResource NavigationMutedTextBrush}"
									FontSize="11"
									Text="{TemplateBinding Role}"
									TextTrimming="CharacterEllipsis" />
								<controls:ConnectionStatusIndicator
									Margin="0,6,0,0"
									Compact="True"
									Foreground="{DynamicResource NavigationMutedTextBrush}"
									State="{TemplateBinding ConnectionState}"
									Status="{TemplateBinding ConnectionStatus}" />
							</StackPanel>

							<Button
								Grid.Column="4"
								Command="{TemplateBinding LogoutCommand}"
								Style="{StaticResource SidebarLogoutButtonStyle}" />
						</Grid>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
